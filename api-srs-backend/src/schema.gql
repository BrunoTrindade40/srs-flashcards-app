# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type DeckCount {
  flashcards: Int!
}

type Deck {
  id: ID!
  title: String!
  description: String
  sourceLanguage: String!
  targetLanguage: String
  isArchived: Boolean!
  createdAt: DateTime!
  updatedAt: DateTime!
  _count: DeckCount
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Flashcard {
  id: ID!
  front: String!
  back: String!
  sourceContext: String
  status: String!
  deckId: ID!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type User {
  id: ID!
  email: String!
  name: String
  isAnonymized: Boolean!
  totalXp: Int!
  lastDataExportAt: DateTime
  lastNotificationSentAt: DateTime
  createdAt: DateTime!
  updatedAt: DateTime!
  dailyNewCardLimit: Int!
  maxDailyReviews: Int!
  currentStreak: Int!
  longestStreak: Int!
  pillReminderTime: String
}

type Query {
  me: User!
  myDecks: [Deck!]!
  deck(id: ID!): Deck!
  deckFlashcards(deckId: ID!): [Flashcard!]!
  dueFlashcards(deckId: ID!): [Flashcard!]!
}

type Mutation {
  updateMySettings(data: UpdateUserSettingsInput!): User!
  createDeck(data: CreateDeckInput!): Deck!
  updateDeck(data: UpdateDeckInput!): Deck!
  archiveDeck(id: ID!): Deck!
  createFlashcard(data: CreateFlashcardInput!): Flashcard!
  updateFlashcard(data: UpdateFlashcardInput!): Flashcard!
  removeFlashcard(id: ID!): Flashcard!
  submitReview(flashcardId: ID!, rating: Int!): Boolean!
}

input UpdateUserSettingsInput {
  dailyNewCardLimit: Int
  maxDailyReviews: Int
  pillReminderTime: String
}

input CreateDeckInput {
  title: String!
  description: String
  sourceLanguage: String = "pt-BR"
  targetLanguage: String
}

input UpdateDeckInput {
  id: ID!
  title: String
  description: String
  sourceLanguage: String
  targetLanguage: String
  isArchived: Boolean
}

input CreateFlashcardInput {
  front: String!
  back: String!
  sourceContext: String
  deckId: ID!
}

input UpdateFlashcardInput {
  front: String
  back: String
  sourceContext: String
  deckId: ID
  id: ID!
}