# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""Agregador de contagem de relações do baralho"""
type DeckCount {
  """Quantidade total de flashcards associados a este baralho"""
  flashcards: Int!
}

"""Modelo principal do baralho de estudos"""
type Deck {
  """Identificador único do baralho (UUID)"""
  id: ID!

  """Título identificador do baralho"""
  title: String!

  """Descrição opcional do propósito do baralho"""
  description: String

  """Idioma de origem do conteúdo"""
  sourceLanguage: String

  """Idioma alvo do aprendizado"""
  targetLanguage: String

  """Sinalizador de arquivamento lógico do baralho"""
  isArchived: Boolean!

  """Vínculo com o identificador único do utilizador criador"""
  creatorId: ID!

  """Timestamp de criação do registo"""
  createdAt: DateTime!

  """Timestamp da última modificação do registo"""
  updatedAt: DateTime!

  """Agregador com contagem de relações"""
  _count: DeckCount
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Flashcard {
  id: ID!
  front: String!
  back: String!
  sourceContext: String
  status: String!
  deckId: ID!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type User {
  id: ID!
  email: String!
  name: String
  isAnonymized: Boolean!
  totalXp: Int!
  lastDataExportAt: DateTime
  lastNotificationSentAt: DateTime
  createdAt: DateTime!
  updatedAt: DateTime!
  dailyNewCardLimit: Int!
  maxDailyReviews: Int!
  currentStreak: Int!
  longestStreak: Int!
  pillReminderTime: String
}

type Query {
  me: User!

  """Lista todos os baralhos ativos pertencentes ao estudante autenticado"""
  myDecks: [Deck!]!

  """Obtém detalhes de um baralho específico por ID"""
  deck(id: ID!): Deck!
  deckFlashcards(deckId: ID!): [Flashcard!]!
  dueFlashcards(deckId: ID!): [Flashcard!]!
}

type Mutation {
  updateMySettings(data: UpdateUserSettingsInput!): User!

  """Cria um novo baralho de estudos associado ao usuário logado"""
  createDeck(data: CreateDeckInput!): Deck!

  """Atualiza os dados estruturais de um baralho existente"""
  updateDeck(data: UpdateDeckInput!): Deck!

  """
  Exclui definitivamente um baralho e seus flashcards associados do sistema
  """
  removeDeck(id: ID!): Boolean!
  createFlashcard(data: CreateFlashcardInput!): Flashcard!
  updateFlashcard(data: UpdateFlashcardInput!): Flashcard!
  removeFlashcard(id: ID!): Flashcard!
  submitReview(flashcardId: ID!, rating: Int!): Boolean!
}

input UpdateUserSettingsInput {
  dailyNewCardLimit: Int
  maxDailyReviews: Int
  pillReminderTime: String
}

input CreateDeckInput {
  title: String!
  description: String
  sourceLanguage: String = "pt-BR"
  targetLanguage: String
}

input UpdateDeckInput {
  """ID do baralho que sofrerá a mutação"""
  id: ID!
  title: String
  description: String
  sourceLanguage: String
  targetLanguage: String
  isArchived: Boolean
}

input CreateFlashcardInput {
  front: String!
  back: String!
  sourceContext: String
  deckId: ID!
}

input UpdateFlashcardInput {
  front: String
  back: String
  sourceContext: String
  deckId: ID
  id: ID!
}