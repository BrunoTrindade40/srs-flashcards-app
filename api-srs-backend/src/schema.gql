# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type DeckCount {
  flashcards: Int!
}

type Deck {
  id: ID!
  title: String!
  description: String
  sourceLanguage: String!
  targetLanguage: String
  isArchived: Boolean!
  createdAt: DateTime!
  updatedAt: DateTime!
  _count: DeckCount
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Flashcard {
  id: ID!
  front: String!
  back: String!
  sourceContext: String
  status: String!
  deckId: ID!
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""Representa o Estudante no sistema SRS"""
type User {
  id: ID!

  """Identificador único vinculado ao Supabase Auth"""
  authId: String!
  name: String
  email: String!

  """Acúmulo de pontos de experiência (Gamificação)"""
  totalXp: Int!
  currentStreak: Int!
}

type Query {
  me(authId: String!): User
  myDecks: [Deck!]!
  deck(id: ID!): Deck!
  deckFlashcards(deckId: ID!): [Flashcard!]!
  dueFlashcards(deckId: String!): [Flashcard!]!
}

type Mutation {
  syncIdentity(authId: String!, email: String!, name: String): User!
  createDeck(data: CreateDeckInput!): Deck!
  updateDeck(data: UpdateDeckInput!): Deck!
  archiveDeck(id: ID!): Deck!
  createFlashcard(data: CreateFlashcardInput!): Flashcard!
  updateFlashcard(data: UpdateFlashcardInput!): Flashcard!
  removeFlashcard(id: ID!): Flashcard!
  submitReview(flashcardId: String!, rating: Int!): Boolean!
}

input CreateDeckInput {
  title: String!
  description: String
  sourceLanguage: String = "pt-BR"
  targetLanguage: String
}

input UpdateDeckInput {
  id: ID!
  title: String
  description: String
  sourceLanguage: String
  targetLanguage: String
  isArchived: Boolean
}

input CreateFlashcardInput {
  front: String!
  back: String!
  sourceContext: String
  deckId: ID!
}

input UpdateFlashcardInput {
  front: String
  back: String
  sourceContext: String
  deckId: ID
  id: ID!
}