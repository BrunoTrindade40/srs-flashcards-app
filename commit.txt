refactor(frontend): harmonizacao visual do layout principal e dashboard para modo claro (UI02)

- refactor(frontend): alteracao do background global em MainLayout.tsx de bg-slate-950 para bg-slate-50, garantindo fluidez de contraste com o Header branco.
- style(frontend): readequacao do Dashboard.tsx para paleta semantica clara (bg-white, text-slate-800, border-slate-200), eliminando ruidos visuais e estabilizando a carga cognitiva.
- perf(frontend): preservacao da renderizacao estrutural exclusiva via Flexbox em toda a malha do painel, em estrita obediencia a base de conhecimento e ao MVP.