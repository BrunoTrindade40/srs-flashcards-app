fix(frontend): resolve falha critica de injecao no motor css do tailwind

- Injeta o pacote '@tailwindcss/typography' no package.json (devDependencies).
- Soluciona o erro HTTP 500 disparado pelo Vite HMR durante a resolucao do index.css.
- Restabelece a renderizacao das classes prose para o MarkdownRenderer.