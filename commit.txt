fix(frontend): estabilizacao de identidade de memoria no useMemo e expurgo de stale closures

- Corrigida a quebra de igualdade referencial (`Referential Equality`) nos hooks useDashboard e useStudyEngine envelopando o operador de fallback (`?? []`) em instâncias nativas de memoização, de acordo com as restrições arquiteturais do linter e do compilador do React.
- Delegou-se a observação da árvore de dependências estritamente aos objetos raízes gerados pelo Apollo Client (`dataDecks` e `data`), prevenindo "thrashing" reativo e mitigando a recriação inútil de arrays na memória RAM do navegador durante os ciclos assíncronos.