refactor(frontend): aprimora a11y do toast e contencao de foco no react 19

- Injeta o atributo 'aria-live="polite"' no ToastProvider garantindo suporte nativo a Screen Readers.
- Simplifica a arvore AST omitindo o sufixo '.Provider' no ToastContext conforme padrao React 19.
- Adiciona Optional Chaining (?.focus) no cleanup do useFocusTrap para prevenir NullReferenceException.